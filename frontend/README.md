# NanoConnect Frontend

Quick scaffold for a Vite + React + Tailwind frontend matching `readme.md` spec.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

This is a minimal placeholder with hero, listing, and recommendation sections.
